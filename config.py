import psycopg2
host = 'localhost'
user = 'postgres'
password = 'Zxc1259663oliver'
db_name = 'prod_data'

# connection = psycopg2.connect(
#                 host=host,
#                 user=user,
#                 password=password,
#                 database=db_name
#             )
# with connection.cursor() as cursor:
#     cursor.execute("""INSERT INTO product (name, description, price) VALUES ('name', 'description', 1);""")
# connection.commit()
# connection.close()
